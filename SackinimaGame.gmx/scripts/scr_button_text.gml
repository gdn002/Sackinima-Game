///scr_center_square(x, y, string, color, alpha);
// !this script can only be used in the draw event!

// x coordanates
txtx = argument0 ;

// y coordanates
txty = argument1;

// what does it say?
say = argument2;

txtcolor = argument3;

txtalpha = argument4;


draw_text_colour(txtx, txty, say, txtcolor, txtcolor, txtcolor, txtcolor, txtalpha); 
