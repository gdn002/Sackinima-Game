///scr_movement();
scr_get_input();
//get input
if (left_key) or (a_key)
    {
    x -= hspd;
    }
    
if (right_key) or (d_key)
    {
    x += hspd;
    }
    
if (down_key) or (s_key)
    {
    y += vspd;
    }
    
if (up_key) or (w_key)
    {
    y -= vspd;
    }
